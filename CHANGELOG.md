# Changelog

所有重要变更记录于此。格式遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，版本遵循 [Semantic Versioning](https://semver.org/spec/v2.0.0.html)。

详细的设计过程、根因分析和测试用例请查阅 Git 历史、源码注释与对应文档；本文件只保留面向使用者的发布摘要。

## [Unreleased]

### Fixed

- 一次全仓缺陷扫描（四路分片深审：工具层、HTTP/服务端、核心桥/CLI、UI/TUI，逐项人工复核证据后修复，修复项由用户从选项中选定）：

- **工具与算法**：`connectivity` 的 `scope` 枚举与实现对齐——此前 schema 只承诺 `auto/local/public`，实现只认内部词表，按 schema 传 `local`（想只探测本地）会被静默改写成默认值 `loopback-and-public`（含公网探测，语义正好相反且无任何报错）；现在 `auto`=安全默认、`local`=仅回环、`public`=仅公网端点，内部覆盖值（`loopback`/`loopback-and-public`/`any`）仍在 schema 中如实列出。`apply_patch` 的 Add File 块拒绝不带 `+` 前缀的正文行——此前这类行被静默丢弃且报 `applied:true`，调用方拿到被截断的文件。`start_process` 的 `ready_pattern` 在真实输出上触发求值预算（如灾难性回溯，空串预检无法发现）时，调用照常返回含 `command_id` 的启动结果并附 `ready_error` 说明——此前报错在进程已 spawn 之后才抛出，调用方拿不到句柄，进程只能靠 `get_process_snapshot` 捞。`read_files` 的 `paths` 上限 20 个（schema 同步声明）并以最多 4 路并发执行、行顺序不变——此前数量无上限且全量并行，"读全部图片/日志"类调用可同时驻留数 GB 的 base64 行，OOM 带走的是整个 Bridge 进程。`edit_block` 对 `edits[i].path` 指向其他文件的写法点名拒绝（此前被静默忽略，调用方以为改了那个文件）；锁计划随之只锁真实编辑的目标文件（此前会对从未被触碰的文件上幻影锁）。
- **进程与脚本**：`set_todos` 强制 schema 承诺的 100 条上限（此前端上不校验，超大列表被持久化、渲染并推送）。`run_script` 超时路径现在带回脚本已产出的 console 输出——此前恒为空数组，而超时恰恰是最需要诊断的路径。
- **配置与 CLI**：`open-bridge config set` 接入与 MCP、控制台共享的 `validateConfigValue`——此前 CLI 是第三条设置写入路径里唯一只做类型转换的，`port 99999`（之后被端口探针误报"已占用"）、负的并发超时（被静默忽略回落默认值）、`publicHealthTimeoutMs 100`（每次隧道启动必然失败）都能存进配置。`open-bridge stop` 的 POSIX 强杀回退先 SIGTERM 宽限 2 秒、再 SIGKILL 并核实结果——此前裸 SIGTERM 会命中目标实例自己的优雅停机守卫（第二次信号是空操作），进程可能活着而命令已称"进程已终止"。
- **隧道**：Tailscale funnel 的 owner 成功路径武装公网 watch——funnel 是 daemon 侧状态、启动器子进程注定退出且退出只清句柄，挂载事后消失（同机另一实例抢注、`tailscale funnel off`、serve 配置重置）时，实例此前永远顶着 `owner` 与死 URL，而 Start 重试只认 `role: none`，恢复路径整体封死；现在 owner 与 follower 共用同一套"连续两次 free 即认领"的 watch，自动重挂。
- **认证与 HTTP**：bearer 门禁只对带转发身份（经隧道、带 `x-forwarded-for`/`x-real-ip`）的请求计失败与锁定——本机直连（本地 MCP 客户端、浏览器调试、控制台体检的匿名探测）共享同一 socket 键，此前开启 auth 后连点 5 次体检就能把本地客户端锁 5 分钟，体检自身也开始报「返回 429，预期 401」的自毒假阳性。peer 代理改写 `Host` 头为对端自身地址——此前原样转发调用方的 Host，本机跨窗口代理（`http://127.0.0.1:8123/mcp/<对端令牌>`）被对端的 Host 白名单 403 挡下（走隧道域名的流量不受影响，因此一直未被察觉）；gate 与 peer 转发的先后关系与 `auth.ts` 注释同步对齐现实：持有者的门禁覆盖自身令牌路径，peer 流量由 peer 自己的门禁治理。`/mcp` 未捕获异常的回答改为固定句子并记入审计（此前把内部 `error.message` 原文回给客户端，该端点公网可达；现代代同样收紧，且补 `skipIfEnded` 防止在 SSE 流开始后追加 JSON 弄脏流）。`/api` 请求体不是合法 JSON 时返回 400（此前 500 加 Node 解析器原文）。OAuth 注册的 200 上限检查移入串行化存储变更内——先读后写的外部检查可被并发注册突破；OAuth 路由内永不可达的 OPTIONS 分支删除（监听器先行应答，两处 CORS 头已经漂移）。
- **状态与性能**：`state.json` 不再每个 MCP 请求全量写两次——会话票据 `lastUsed` 触摸改为 5 秒尾随防抖、使用统计改为 2 秒尾随防抖（重置与停机仍立即落盘），prune 未移除任何条目时不再无条件持久化。此前高频可丢的计数数据走的是 secrets 级崩溃安全写法（跨进程锁 + 全文重读 + fsync + rename）；共享数据目录时双实例还会在锁上相撞，锁循环耗尽后 persist 尾巴静默吞掉更新。
- **会话生命周期**：`stopInternal` 的 `stopping` 标志改由 try/finally 复位——中途同步异常（如 `transport.close()` 拒绝）此前会把它永久卡住，之后每次隧道重连调度都是空操作。`openLegacySession` 在 attach 失败时回滚会话与票据注册——此前留下半绑定的僵尸会话，后续请求不断刷新其 `lastUsed`，空闲回收永远收不走，只有 Stop 能清。
- **Web 控制台**：状态页轮询失败显示错误横幅、恢复即清除——此前两个轮询都吞错，进程死亡或重启期间页面可冻结在「运行中」、顶栏「已连接」，与日志页的「已断开，重连中」同屏矛盾。服务页错误横幅沿用会话页的"仅清除轮询写入的提示"模式（此前一次重启期间的失败提示永不消失，还会覆盖操作反馈）；统计页补齐同一模式，清零动作补 `poll.invalidate()` 守卫（清零后的计数不再被旧轮询复活）。`/api/settings` 首次加载失败自动重试（最多 5 次，每次尝试都会提示，成功即停）——此前一次瞬断就让安全页与设置页永久停在骨架屏，唯一的提示是一条 2.6 秒即逝的 toast。安全页总览的「当前状态」渲染本地化标签（此前直接显示原始枚举 `public-open`）。Bearer 门禁开关补 busy 防抖（连续两次切换不再可能让开关显示与服务端相反）。日志页「暂停」改为缓冲（上限同视图 800 行）并在继续时合并——此前暂停期间写入的行被静默丢弃，与按钮字面义相悖。清除死注释（引用已移除的「刷新本页」按钮）与死字段（`SettingsActionResult.healthLines`/`healthOk`，无产无消）。
- `docs/tools.md` 同步两处：`read_files` 的单次路径上限与并发行为；`start_process` 就绪检查失败时的 `ready_error` 契约。

## [1.4.1] — 2026-09-25

### Fixed

- MCP 请求追踪行补记 HTTP 方法（`legacy/POST initialize · HTTP 200 · …`）：此前一行只有协议世代与 MCP 方法，一条被长保持的交换看不出是规范内的 GET 流还是卡住的 POST，排障只能翻原始日志。方法走封闭词表（GET/POST/DELETE，其余归 `other`），不引入可注入字段。
- `read_process_output` 等分页读取不再把 ANSI 转义序列从中间撕开：此前一页结尾落在转义序列内（如 256 KiB 分页遇到彩色输出的 `[38;5;196m`）会在两页各留一段裸 ESC 残片进入模型上下文；现在读取在转义起始处收尾并回拨游标，下一页原样补齐完整序列（序列超过 256 字节边界时放弃保留，行为有界）。回环缓冲的 `tail` 窗口同享此修复。

## [1.4.0] — 2026-09-25

### Added

- `bridge_status` 概览新增 `instructions_bytes` 与 `catalog_bytes`：连接期前缀（发现时下发的 `instructions`，以及 `tools/list` 序列化后的目录）各自的 UTF-8 字节数。这两个值就是客户端提示词缓存按字节计费的前缀，此前无人能读到，膨胀只能靠账单发现；现在可以直接对比，也能判断两次相同请求之间前缀是否变过。字节而非字符，因为中文说明在 UTF-8 下约为三倍成本。
- 前缀字节稳定性与 MCP SDK 依赖契约进入测试：目录与说明重复装配、跨协议世代、跨进程重启都必须逐字节相同，两世代 `tools/list` 的字节一致性从源码注释升级为断言；三个 `@modelcontextprotocol/*` 包被依赖的行为假设（`instructions` 原样下发、目录顺序与 annotations 不被改写、v2 以方法名字符串路由）各有指名断言，升级依赖前必须全绿。这类回归不会让任何功能失效，只会让每个客户端的缓存静默失效。
- 新增 `open-bridge diagnostics [--out FILE]`：把数据目录工件投影成一份可直接贴进 issue 的脱敏诊断（工件清单 + 行为骨架 + 阈值发现），默认覆盖式写入 `~/.open-bridge/diagnostics.md`。它读磁盘而不问活实例，所以实例卡死或起不来时照样能生成。公开性靠构造而非擦洗——只输出自己点名的字段，从不转贴 `audit.log` 或 `bridge.log` 的任何一行，因为现有脱敏是黑名单式的，对路径与命令文本无话可说；报表结尾会列出「不含什么」。新增 `docs/observability.md`：四个观测表面的分工、数据目录工件速查表（看什么·健康·不健康）、发现项对照与维护规范。

### Fixed

- 一次全仓缺陷扫描（核心热路径精读 + 五路分片审计，逐项复核后修复 16 项）：

- **进程与服务**：`service {action:"stop_all"}` 在终止未确认（进程 5 秒预算内未退）时不再清空 `commandId`，与单个 `stop` 的契约对齐——此前孤儿进程对 service_status 隐身，下一次 start 会起第二个实例抢端口，行内附重试指引。`set_process_policy` 关闭自动重启时同步取消已排期的重启并释放 `resource_keys` 租约（此前只清定时器，锁被死进程占用最长 1 小时，后续声明同键的调用全部等满 `waitTimeoutMs`）；该操作不是停止，进程记录保留退出码与崩溃事件。`process_control {action:"restart"}` 现在把 `teeLogPath` 带给替换进程（与自动重启路径一致），手动重启服务进程后 `read_service_log` 不再静默停在重启前。`close_shell` 的手工收尾补写 `endedAt`：此前已关闭 shell 的记录永远没有结束时刻，`uptime_ms` 随每次轮询无限增长。

- **文件与算法**：`read_files` 的 `max_bytes: 0` 现在如实报 `truncated: true`——此前返回空内容却声称"未截断"且无续读游标，非空文件看起来像"完整读到了空"，极易诱导客户端随后无守卫覆盖写。TUI「变更」页把 staged 重命名显示为新路径（真实 git 的 porcelain -z 是新路径在前；旧实现取第二个 token 显示旧路径，Enter 打开的 diff 把重命名读成"整文件删除"；单测夹具此前把 git 的字段顺序写反，已连同 numstat 连接键一并修正）。`apply_patch`/`edit_block` 的增删统计不再漏计以 `++`/`--` 开头的内容行（渲染成 `+++ foo` 的真实变更曾被当作 diff 头跳过，统计偏小或为 0）。`read_files` 越过唯一一条超长行（无尾随换行、块对齐）时 `lines_total` 不再少计最后一行——此前 `lines_total: 0` 与 `reached_eof: true`、全文件 sha256 同报，自相矛盾。

- **Web 控制台与认证**：任务页「多久前完成」恢复显示（服务端发 `completed_at`、控制台读 `completedAt` 的字段错位，功能自上线以来实际未生效；控制台已对齐线上契约的 snake_case）。OAuth 注册达到 200 上限的 429 文案改为指向真实存在的恢复路径（停止实例后从 `secrets.json` 的 `openBridge.oauth` 记录中删除客户端条目；原文案指的"控制台吊销客户端"能力并不存在），`docs/configuration.md` 补充同样说明。安全页令牌的「创建/过期」时间改用本地时间渲染（此前直接截取 UTC 串，UTC+8 用户看到的有效期差 8 小时）。会话页轮询恢复后清除错误横幅（此前一次短暂 503 的提示永不消失）。

- **隧道与 CLI**：公网域名 watch 链改用代数令牌退休——停止、切换提供商或 claim 让位都能终止"一轮探测在途"的链；owner 的 mount 不再被幽灵链的健康探测降级为 follower（"Published through a peer tunnel" 指向自己的 mount），停机中的实例也不会被链尾的 claim 整体复活重启。CLI 解析器对已知取值旗标（`--home`/`--root`/`--port`/`--label`/`--ttl`/`--pid`/`--out`/`--tail`）的裸旗标直接报用法错误：此前 `token create --ttl` 铸出 1 秒过期的令牌、`stop --pid` 静默改为停当前目录实例、`serve --root` 死于 `path.resolve(true)`。Tailscale 启动路径对 `funnel status` 无从回答（unknown）时改为跟随+等待而非直接 `funnel --bg`——不可读的回答从来不是空闲的证据，此前一次超时就会顶掉活对端的 443 挂载。`pidAlive` 把 EPERM 视为"存在"（仅 ESRCH 判死）：管理员终端启动的实例在普通终端的 `instances`/`stop --pid`/同目录 `serve` 锁判活不再误判，CLI 与隧道 peer 注册表共用同一实现。

- **同批扫描的遗留小项**：`terminateProcess` 不再在终止请求时提前释放 `resource_keys` 租约——存活进程的资源保持被声明直到 close 释放（终止失败的幸存者此前处于「进程活着、锁已空闲」状态，可被二次声明抢同一端口）；仅已退出且挂着待重启的命令由取消动作释放租约。`search_files` 内置引擎不再剥掉文件末行（无换行结尾）的尾随 CR，与 ripgrep / `read_files` 的结果一致。`activity_log` 搜索的 `limit` 现在要求 1–500 的整数并按名拒绝（此前 `limit: 0` 被静默改写成 50 行整页）。控制台英文界面不再出现服务端下发的中文串：顶栏状态徽标改为结构化 `status` 由 UI 按页面语言渲染，TTL 下拉补齐英文标签（`labelEn`），令牌弹窗的有效期改为数字 `ttl_seconds` 由 UI 渲染。文档修正两处：`docs/tools.md` 的 restart `command_id` 说明改为实际行为（复用原 id，而非返回新 id）；`docs/configuration.md` 的设置子页改为实际的六个并注明日志轮转位于 `/console/logs`。
- **二轮扫描（此前未覆盖的 src/host、src/workspace、src/shell、src/process 切片）**：`SharedJsonStore` 锁内合并改为磁盘优先——此前本进程内存会压过锁内重读的磁盘内容，多实例共享数据目录时并发写入会静默回滚对方已成功提交的键（路由令牌、设置项、使用计数都在受影响面上）；被拒绝的写入现在会撤回内存中的键，不再搭下一次成功写入的车。`config.json` / `state.json` / `secrets.json` 读取前剥离 UTF-8 BOM：PowerShell 5.1 与旧版记事本手编出的带 BOM 文件此前会让整份配置静默回退默认值，并被下一次写入永久覆盖。
- WSL 的 `System32\bash.exe` 不再被检测为「Git Bash」：装有 WSL 而未装 Git Bash 的机器此前会把全部命令静默送进 WSL Linux（Windows PATH/PATHEXT 失效、自定义 env 被丢弃、服务日志 tee 把 `C:/...` 当相对路径写进工作区），现自动回落 PowerShell。`send_to_shell` 拒绝以续行符（`\`）或不完整管道（`|`、`&&`）结尾的命令——此前哨兵行会被吞掉，会话永久卡在 pendingMarker，只能关闭重开；单个 `&` 后台作业不受影响。`interact_with_process` 拒绝指向持久 shell 会话（其 stdin 归 `send_to_shell` 的队列与哨兵跟踪所有，旁路写入会交错输出并绕过完成标记）。
- `tunnel-integration` 集成测试的公网边缘改为确定性夹具（预载 fetch 拦截），消除对真实网络的负载敏感抖动。
- 清除测试夹具中误用的真实 MCP 路由令牌与实例域名，统一改为明显的虚构数据。新增覆盖源码、测试和文档的凭据检查，接入 CI 与 `release:check`，失败信息只列位置、不回显凭据；本地运行数据与敏感文件加入忽略规则。曾公开的令牌必须轮换，仅修改仓库文件不能使其失效。

- 修复 `activity_log` 搜索将 `warning` 审计行误报为 `completed`、按 `warning` 筛选漏掉警告的问题。写入端、审计读取端和 TUI 共用状态词表；未知状态的既有兜底行为不变。

- `open-bridge logs --follow` 在 Linux 下不再永久孤儿：零字节写探针在 POSIX 上探测不到断管（内核对空写返回成功），静默日志的 follower 在 `| head` 退出后继续常驻。现以只读哨兵 socket 监听写端的 EPOLLERR/EPOLLHUP，读到断管即退出；健康管道、文件重定向与 TTY 行为不变，Windows 路径不变。
- `config.json` / `state.json` / `secrets.json` 的写入在 rename 前补一次尽力而为的 `fsync`：rename 对并发读者是原子的，对崩溃不是，掉电后可能只剩目录项而没有数据，`secrets.json` 变空即路由令牌不可复原——客户端手里的 URL 全部失效且界面上看不出原因。不支持 sync 的文件系统会忽略这一步，写入照常发布，不会因为无法 sync 而失败；工作区普通文件的写入路径刻意不同步（见 `src/workspace/persist.ts` 的说明），避免每次写文件都付一次 sync。

### Changed

- `run_script` 脚本内未知工具的拼写建议与 dispatcher 的未知工具报错统一为同一实现与同一格式（`Did you mean: read_files, patch_file?`）。此前脚本路径自带的建议是另一种措辞（带引号、无冒号），同一类错误在两个表面长得不一样。
- `open-bridge --help` 的 `serve` 与 `instances` 行现在标出各自已有的别名（`start` / `list`）；别名一直存在，只是帮助文本从未提及。

## [1.3.0] — 2026-09-22

### Added

- 任务面板时间化：条目转为完成时盖 `completedAt`（TUI 任务行右列显示完成时刻、控制台任务页显示「多久前完成」）；TUI 任务标题栏显示「更新 HH:MM:SS」，有进行中任务却超 10 分钟无写入时以预警色提示「⚠ N 分钟未更新」。
- TUI 活动页重做：事件一律单行显示（超宽以 `...` 截断，不再软换行）；`↑↓` 光标选择、`Enter` 展开事件全文（`Esc` 返回、详情内 `Tab` 失效）；详情页内容自动换行、超出面板可翻页滚动到末尾；`mcp`/`process` 生命周期事件不再被过滤，以弱化色入列，杜绝「有调用没记录」的观感。
- TUI 变更页改为单行文件光标：`↑↓` 选择文件、`Enter` 打开该文件相对 `HEAD` 的工作树 diff（含未跟踪与二进制文件，`Esc` 返回）；原有 `d` 累计 review diff 继续保留。
- 任务面板完成时刻与标题拉开 2 列空隙、并改用不同灰阶（标题 muted、时刻 dim），长标题不再把时刻挤贴到正文。

### Fixed

- TUI 活动事件改用稳定 ID，并以调用关联 ID 精确合并起止记录：实时插入新事件不再让历史光标漂移，同毫秒同工具事件不会打开错详情，并行同工具调用乱序完成时也不会串错参数与耗时。
- TUI 运行进程按最长运行优先展示（此前比较器方向相反）；控制台工具、技能与体检首次加载失败后停止显示永久骨架屏，直接呈现错误并允许后续成功刷新清除旧错误。
- TUI 活动行时长列固定宽度：运行中行的时长逐秒变化（9s→10s、59s→1m00s）会移动正文软换行边界，面板底部行因此偶发翻转闪烁。
- 任务面板：标题栏「更新」时刻与完成行时钟右缘同列对齐（此前行内时钟多缩进一列）；隧道标签去冗余，`ngrok 公网 ●` → `ngrok`（tailscale 同理，状态点一并去掉）。

### Changed

- TUI 变更页标题提示「d 预览 diff」，diff 预览页提示「Esc 返回变更」；预览内 Tab 不再切走视图，Esc 是预览唯一出口（滚动键不变）。

## [1.2.0] — 2026-09-22

### Added

- 控制台新增「技能」页：与 `list_skills` 工具同源展示发现的技能（名称、说明、来源目录、工作区外标记），支持按名称/说明过滤与复制 SKILL.md 路径；空库时说明扫描目录与 SKILL.md 约定。
- TUI 变更页新增累计 diff 预览（按 d）：走 review_changes 只读面，+/-/@@ 着色、超长截断标注、可滚动，q/Esc/Tab 返回；不推进审阅基线。

## [1.1.0] — 2026-09-21

### Changed

- 仓库根目录精简：Windows 启动脚本移到 `scripts/`，架构说明移到 `docs/ARCHITECTURE.md`。`*.cmd` 固定为 CRLF，避免 Linux CI 把启动器误判成 LF。
- TUI 各页标题不再写「Tab 任务 / Tab 变更 / Tab 返回活动」。Tab 仍是切页键，标题只保留当前视图名；滚出顶部时仍显示行数提示。
- TUI 任务面板标题栏进度展示精简：移除冗余的「· N 进行中」，保留直观进度条与完成统计（`▓▓░░░░ 25% · 1/4 完成`）。
- TUI 侧栏任务行改为展示推进进度（`已完成/总数（百分比%）`），代替原先仅有总数与进行中数量的格式（`5（1 进行中）`）；窄窗概览卡片同步跟进。
- TUI 顶栏布局精简与工作区常驻：版本栏简化为仅显示版本号（`v1.0.0`），移除冗余的产品名、端口号与前置菱形符号（`◆`）；中央居中直接完整展示当前工作区目录，侧栏概览不再重复截断展示。Web 控制台「实时状态」卡片同步展示工作区目录（`workspace_root`）。
- TUI 侧栏隧道状态具体化：公网隧道状态由原先抽象的「公网 ●」优化为具体提供商标注（如 `ngrok 公网 ●`、`tailscale 公网 ●`，跟随状态亦同步对应为 `跟随 ngrok` / `跟随 tailscale`），使操作者对隧道链路一目了然。
- TUI 地址下移至侧栏底部与工作台全屏纵向扩展：将原本位于屏幕最下方的控制台地址迁移至左侧栏最底部，移除易造成多行折行杂乱的超长 MCP 地址，仅保留 Web 控制台入口（去除 `/console` 末尾斜杠）；取消宽屏工作台下占满全宽的独立底部页脚（Footer），使右侧活动/任务/变更面板多获得 2 行显示高度，视觉更紧凑自然。窄屏/极小窗口下依然保留底部页脚回退展示。
- TUI 活动面板扩展高频工具语义化摘要提取（文件操作动词与路径、代码修改处数、服务启停与分组、无参数巡检工具中文标签、网络目标探测、终端会话、延时等待、任务数量、瞬时进度等），使事件行展示人类友好的具体动作而非空白。
- 活动记录保留与滚动体验优化：活动在内存与 TUI 视图中的缓冲容量从 40 提高至 200 条；当活动超出面板行数且位于顶部时，标题栏增加溢出下翻提示（`↓N 行 · PgDn 下翻`），消除活动被吞没的困惑。
- 接入提示词文案精简：控制台「复制接入提示词」的固定文本缩为两句极简版（连接指令＋任务面板与传输层约定），细节约定交由客户端连接时阅读的服务器说明；URL 原样给出，去掉【】包裹符号。
- 文档：`docs/configuration.md` 补全 `get_config` 实际返回而参考漏记的配置键（隧道重连与健康检查、Bark 与声音键名、令牌 TTL、OAuth 重定向白名单、Shell 与并发、`toolProfile`、`allowedDirectories`）。

## [1.0.0] — 2026-09-21

### Added

- TUI Tab 第三页「变更」：列出每个工作区文件的路径与各自 +/-（未跟踪单独标注），侧栏摘要行保留；Tab 在活动 / 任务 / 变更间循环，Esc 仍只回当前页顶部。
- TUI 任务视图：按 Tab 在「活动 / 任务」间切换右侧宽面板。任务视图完整保留任务，长标题按行宽自动折行；↑↓ / PgUp / PgDn / Home / End 滚动当前视图，活动与任务各自保留位置，Tab 仍是唯一切换键（Esc 只回当前视图顶部）。窄窗或矮窗下任务独占内容区，缩放、列表缩短后自动钳制滚动位置；空列表明确显示「暂无任务」。侧栏只保留一行「任务 N（M 进行中）」摘要，不再截断标题。任务与 Web 控制台同源（`set_todos`），重启后从持久化存储恢复。
- TUI 侧栏新增常驻「变更」行：自最近一次 git commit 以来的工作区变更摘要（`+插入 -删除 · N 文件`，新增数绿色、删除数红色，含未跟踪文件按整文件计行），每 5 秒异步刷新、渲染帧只读缓存；干净的工作树显示「干净」而非隐藏（缺失无法区分干净与未监控），非 git 仓库如实显示「非 git」。
- TUI 底栏新增网页控制台入口（`http://127.0.0.1:端口/console/`）：控制台仅应答本机回环请求，入口固定显示本地地址，与 MCP 地址同栏展示。
- 新增文档覆盖回归门禁：docs/tools.md 与 MCP 工具定义强制一一对应——每个已发布的工具必须有文档条目（`name` 或 **name** 两种格式皆可），文档粗体条目也不得承诺未定义的工具。此前这对账只能手工进行，而一次手工 grep 比对就同时漏了 4 个定义、多出 1 个幻影名。
- serve 终端 TUI 工作台布局（阶段三）：宽终端（≥76×22）下左侧固定侧栏（状态/隧道/调用/会话容量/进程/服务概览，每进程输出缓冲水位、服务启停列表）+ 右侧活动面板（↑↓/PgUp/PgDn/Home/End 滚动浏览历史，End/Esc 回到最新并跟随；离开最新时面板标题显示「↑N 行 · End 回底」）。窄窗自动回退堆叠布局。TUI 定位保持只读观看，设置/后台操作仍在 Web 控制台；滚动键经 raw mode 读取，Ctrl+C 在 raw mode 下显式转发真实 SIGINT，优雅关停路径不变，所有终端模式（备用屏/光标/raw stdin）在停止与退出时双重恢复。滚动与几何为纯函数，12 项单测锁定。

### Changed

- 2025 世代 MCP 会话在重启后可恢复：已签发的 `mcp-session-id` 以票根持久化，进程退出或连接断开不会作废；空闲超时与客户端 DELETE 仍使旧 id 失效。从未签发的 id 继续 404。
- 网页 AI 提示词与 MCP 连接说明要求多步工作主动维护 `set_todos`：操作者 TUI 任务面板只显示这份清单；`report_progress` 不能代替它。
- TUI 底栏改为一行一个地址：控制台与 MCP 地址各占整行，小屏幕不再把 MCP 地址截成省略号；同时移除操作提示行（关闭终端窗口即停止，滚动提示由面板标题在滚动时显示）。
- TUI 会话计数不再显示容量上限（如 0/64）：上限是开发者知识，操作者只需知道当前连接数与活跃数。
- TUI 的调用/成功/失败计数改为本次启动以来的计数：持久化的历史统计（跨重启保留、供 Web 控制台诊断）不再进入 TUI，刚启动的实例不再继承旧数字。
- TUI 底栏 MCP 地址完整显示（含路由 token），与启动横幅、`open-bridge url` 输出一致；此前遮蔽为 `<redacted>`，地址无法直接复制使用。
- 移除 TUI 底栏「Ctrl+C 停止」提示：关闭终端窗口即停止服务，提示属于噪音；滚动方式、日志路径与 `--no-tui` 提示保留。

### Fixed

- TUI 活动流改为操作者摘要：隐藏 MCP 传输行和进程生命周期行；参数只抽出 command/query/path，不展示 JSON、timeout 或 `&&` 后续段；`Request received` / `Completed in N ms` 不再占用一行。长消息按空格/路径分隔符折行。
- 修准 TUI 侧栏 Git 变更摘要：未跟踪目录展开为文件计数；空文件与尾换行按 git 行数计（不再被 `split('\n')` 夸大）；含空格等特殊路径不再被 trim/去引号弄丢；二进制未跟踪不计行；git 超时或读取失败显示「读取失败」，不再冒充「非 git」。干净工作树与红绿配色保持不变。
- 修复 TUI 满行写入后再擦除造成的右边界丢字（如「运行中」「Tab 返回活动」尾字消失）：改为逐行绝对定位、先清行后绘制，不依赖换行/自动折返，也不在新内容后执行擦除。
- 修复活动消息、命令摘要等文本携带换行或终端控制序列时串到侧栏、挤掉顶栏的问题。行内文本保留可读内容并净化控制符，任务标题仍按真实换行与列宽折行；颜色由渲染器统一添加。
- 为 `batch` / `run_script` 子调用补齐真实的成功/失败结束记录：已结束的调用不再在 TUI 中永久转圈计时；保留工具返回结构、原始错误及顶层请求的唯一计数口径。
- 修复 TUI 任务摘要只统计前 16 条、后续进行中任务被漏计的问题；任务列表不再受该快照上限截断。任务长标题的图标留白按实际列宽计算，中文终端及多行标题不会挤掉尾字或突破帧高。
- 修复 TUI 活动面板跟随反转：跟随哨兵误钳到数组尾部，而事件数组是最新在前——事件数一旦超过面板行数，跟随会锁死在最旧的事件窗口、新调用不再上屏。现在面板保持最新在顶（向下滚动浏览更旧历史），跟随与 Home/Esc 锁定最新事件，End 跳到最旧历史，离开顶部时面板标题显示「↑N 行 · Home 回顶」；窄窗堆叠布局的活动流同样改为截取最新事件而非最旧。
- TUI 信息去重，一处事实一处：顶栏独占身份/端口/状态胶囊，侧栏独占计数与隧道，底栏独占 MCP 地址与操作提示；移除底栏重复的会话/调用/端口/工作区名与侧栏重复的「状态」字段，无进程时不再渲染空的「进程」段。
- TUI「运行」时长改用本次进程的实际启动时刻：此前误用跨重启持久化的统计窗口起点，Bridge 刚启动也会显示数十小时。
- CJK 终端（zh/ja/ko 区域设置）下模糊宽度字符（·、…、─、●、◆、⚠、✓、✕ 等）按 2 列计宽：盒子边框、分隔列与横向填充线的几何改为按实测字符宽度计算并在截断后补齐，修复行尾字符被终端吃掉（如「● 运行」丢「中」、日志路径截尾缺字符）；非 CJK 终端行为不变。

- 修复 TUI 活动流「永远计时」：invoke 行与结局行的配对此前依赖双方都携带参数摘要，而结局行（由 MCP 端点记录）从不携带——配对永不成立，每个已完成的工具调用都显示为永久转圈的进行中，计时虚涨；`process · Started` 生命周期行则根本没有对应的结局行。现在结局行按工具名 FIFO 关闭最老的未闭合 invoke 并显示真实耗时；`process` 行改从命令表读取真实状态——存活进程照常计时，已结束进程显示真实存活时长（endedAt−startedAt），已清理的记录降级为中性标记、不虚构时间。仅影响显示，审计日志不变。
- serve 终端新增 ainovel-cli 风格的 TUI 仪表盘（阶段一）：启动横幅之后进入备用屏幕，500ms 就地重绘——顶栏（版本 · 端口 · 隧道 · 工作区名 · 状态胶囊，繁忙时胶囊图标切换为实时 spinner）、概览卡（运行时长 / 调用 / 会话容量 / 进程 / 服务）、进程卡（运行中命令 + 输出缓冲水位条）、活动流（时间戳 + ✓/✕/spinner 三态图标；invoke 行在其结局落地时合并为携带真实耗时的一行，未观测到配对则不虚构耗时）、底部用量状态栏。健康度渐变沿用 ainovel-cli 的阈值（<70% 绿 / 70–84% 黄 / ≥85% 红），应用于会话容量与每命令输出缓冲占用。Ctrl+C 语义不变（无 raw mode），停止时恢复原始终端；无真实控制台（服务 / CI / 重定向）或 `--no-tui` 时保持原输出路径。渲染层为纯函数（CJK 感知列宽、精确几何、窄窗降级阶梯），由单测锁定。

## [1.0.0-rc.2] — 2026-09-20

### Changed

- `list_directory` / `search_files` 的已公布 inputSchema 与运行时对齐：`depth` 移除 1–3 枚举（小于 1 钳制为 1、更大的值按请求递归），`context` 移除 0–20 上下限（超界钳制）——运行时接受的合法调用不再违反自己公布的契约。
- 控制台统计页措辞诚实化：调用计数按工作区持久化、跨 Bridge 重启保留，「本次运行」改为「自统计开始」、「运行时长」改为「统计时长」。
- docs/tools.md 补齐：`open_shell` 新开/复用两种结果形状、`send_to_shell` 结果 `cwd` 的含义（启动目录，不跟踪 shell 内 `cd`）、`write_file` 响应 `mode` 的取值、`get_usage_stats` 的统计窗口语义、`run_script` 预算钳制区间。

### Fixed

- `find_files` 的 `**/` 现在只按**完整路径段**匹配（或匹配零段）：`**/host.ts` 不再误中 `node-host.ts`；裸 `**` 照旧可跨分隔符。
- 创建/删除文件的结果 diff（`apply_patch`、`edit_block` 等）不再产生幻影空行与多计一次增删：空的一侧按 0 行处理，`\ No newline at end of file` 只在真实缺少尾换行时出现，与 git 一致。
- `file_op{op:"create_directory"}` 的 `created` 如实反映**本次调用**是否建了目录：目录已存在时返回 `created: false`（此前恒为 `true`）。
- `write_file` 的 `mode` 与 `read_files` 的 `encoding` 收到非法值时报 `Invalid` 并列出合法值，不再静默回退（此前 `mode:"Append"` 会按覆盖写、拼错的 `encoding` 会按 utf8 读二进制）。
- `activity_log{action:"recent"}` 的 `at` 与 `search`、审计日志、`/api/activity` 一致，为 ISO-8601 UTC（此前是服务器本地时间串）；控制台活动视图仍显示本地时间（改在渲染层格式化）。
- 文档与实现对齐：`read_files` 的 `sha256` 尾部预算行为、`edit_block` 的 `replace_all` 参数、`process_control{action:"restart"}` 返回新 `command_id` 均已写明。
- 修复 Windows + Git Bash 下停止/重启/删除被监控命令时误报「无法终止」的问题：MSYS 的 fork/exec 模拟会切断 Windows 父子链接，后台子进程（如 `while` 循环里的 `sleep 30 &`）成为 `taskkill /T` 与进程树枚举都看不见的孤儿，占住 stdio 管道导致 close 永不触发、5 秒预算耗尽。现在对 bash 类 shell 先枚举 Windows 可见成员，再经 `ps -W` 的 PGID 按 MSYS 进程组整组 `kill -9`（严格跳过组 0，防止波及无关系统进程），最后 taskkill 收尾；原生 shell（PowerShell/cmd）改为单次原子 `taskkill /T /F`。附 win32 门控的进程树集成测试。
- 修复 `close_shell` 泄漏后台任务：Windows + Git Bash 下关闭持久 shell 返回 `closed:true`，但会话里的后台任务（`npm run dev &`、`sleep 300 &` 等）继续存活——bash 优雅退出不带走后台子进程，令整个 kill 块被跳过；即使执行到，`taskkill /T` 也看不见 MSYS 孤儿。现在 win32 的 bash 类会话直接进入与被监控命令终止共享的家族击杀路径（`killWindowsProcessFamily`：枚举 → MSYS 进程组组杀 → taskkill），两处不再各自漂移。附 win32 门控集成测试（改名副本 ob-sleep 防并行文件干扰）。
- 修复 `open-bridge stop` 兜底路径泄漏后台任务：目标实例无法响应 HTTP 优雅停止时，CLI 直接`taskkill /T /F` 实例 pid——只能杀到 Windows 可见进程树，实例 bash 会话/受管命令的 MSYS 孤儿（`npm run dev &`、watcher）继续存活为无主游魂（与 terminateProcess/close_shell 同根因，第三处同型点）。家族击杀逻辑抽出为无 host 依赖的 `src/process/win-family-kill.ts`，terminateProcess、closeShell 与 CLI 兜底三处共用同一实现；CLI 侧以 autoShell()（与未配置时桥自身解析的首选 shell 一致）执行组清扫，并在事后核验实例确实退出而非信任被吞掉的逐成员错误。附 win32 门控集成测试（runtime 记录指向死端口模拟挂死 + 改名副本 cli-orphan 防并行文件干扰）。

## [1.0.0-rc.1] — 2026-09-20

### Added

- 新增文档回归检查，识别失效的源码指针和示例中的真实 MCP 连接凭据；现行文档变更进入 CI 验证。
- 新增 `npm run package:check` 与 `npm run release:check`：发布前检查 npm 实际打包清单，确保 CLI、编译产物和用户文档齐全，且不误发源码、测试和工作流文件；CI 同步执行该检查。
- 为长命令的后台监管结果新增 `ready_checked`，明确区分“已观察到就绪”与“未请求就绪检查”；工具契约和文档同步说明基于 `command_id` 续读，避免传输超时后重发有副作用的命令。
- 补全 `get_config` 的 MCP 输出 schema：工具档、认证、OAuth、并发、通知、声音和日志等实际返回字段现均有机器可读类型声明。
- 新增可选的 **Tailscale Funnel** 公网隧道提供商；保留既有 ngrok 与 `--no-tunnel` 路径。
- 新增 `start-open-bridge-project.cmd`：双击后固定以本项目为工作区、使用端口 **8123**，构建后在终端显示控制台地址但不自动打开浏览器。

### Changed

- 精简 Agent/贡献/PR 约定，区分缺陷回归与新增覆盖，并明确说明注入、任务授权及重启边界；问题报告模板提醒先脱敏。
- 移除无生产调用的旧通知、控件元数据、单注册表查找和数组搜索上下文实现；测试改为验证真实 schema、设置入口和搜索结果，测试夹具不再随运行代码发布。
- 清理已过时的迁移/对照待办，更新架构、接入、路由令牌与日志存储说明；协作指南改为不含实例凭据和个人机器路径的可复用流程。
- 移除无调用者的内部 CLI 语言设置入口，保留环境变量/系统 locale 的语言选择，以及现有协议与工具别名兼容。
- 手机通知收敛为两个明确事件：`waiting`（真正等待回答/选择）和 `finished`（本轮结束）。每个工作 episode 最多一次 Bark 请求，固定使用持续、time-sensitive 的投递形态。
- 对会暂停回合的提问/选择，连接指令要求在展示 UI 前或同一回合发送 `waiting`。
- 控制台隧道设置统一为“提供商、状态与操作、高级设置”三段；自动配置只填空值，公网可达性可在控制台验证。
- 连接时会说明实际用于执行命令的 shell 与方言，避免模型按错误语法运行命令。

### Removed

- 移除 Electron 桌面客户端及其构建链；项目保持 CLI + MCP 服务 + Web 控制台。
- 移除 MCP 工具结果中的“行为教练”提示层。工具、todo 看板和批处理能力不受影响，但模型不再被连续调用提示和过时的 todo 通知说法打扰。
- 移除旧的通知路由、重复投递、进度/注意力事件和 todo 自动推送配置。

### Fixed

- 修复控制台 ngrok 手动域名无法保存、已保存的自定义域名刷新后隐藏，以及无法显式清除域名的问题；明确空值在下次启动时仅本机可用，移除不受支持的随机地址承诺，保留严格主机名校验。
- 修复较早的保存回包覆盖新输入草稿；手动域名保存失败或保存期间继续输入时保留当前草稿。
- 公网可达测试必须有成功的公网探测才显示可达；未发布、未探测与探测失败分开显示，保留暴露风险提示。
- 会话页补充现代 MCP 无状态活动汇总与进行中请求数，区分首次观察和握手时间；不虚构会话 ID、调用/待办总数或断开操作，状态页明确仅统计有状态会话。
- 修复会话断开或回收后任务看板仍显示实时的问题，保留历史计划与进展并标注离线。
- 服务操作按服务分别防重入，允许不同服务并行操作，完成或失败不会提前解锁其他仍在处理的服务。
- 控制台写操作遇到无效成功响应时明确提示结果未知并保留原页面状态，不自动重发可能已经执行的请求。


- 修复通知设置页：折叠与展开状态都显示「手机/本机声音」状态徽章；Bark 设备密钥在锁定时新增只读视觉反馈；按钮 disabled 同时降饱和度便于辨识；「什么时候提醒你」从可折叠改为静态描述卡（无可揭示内容）；Bark 密钥掩码格式改为 `前缀4 … 8 个点 … 后缀2`，避免前后省略号与圆点合并。新增对应 UI 测试。

- 审计依赖、打包清单与发布资产：`@modelcontextprotocol/sdk`、`@modelcontextprotocol/server`、`@modelcontextprotocol/node` 各自是必要的 v1/v2 协议直接依赖；其余 devDep 全部由 vitest/vite/jsdom 与 typescript-eslint 实际使用，无可删除的冗余。`scripts/`、`vendor/`、`package.json` `files` 与 package-check 三方一致；当前没有发现需要删除或更新的过期资产。
- 补全行为准则遗留的举报联系方式占位符，使用项目已公开的维护者联系渠道，并添加回归检查。
- 加固文件复制、补丁、路径、自毁保护和换行处理，避免静默覆盖或错误 diff。
- 修复 OAuth 令牌撤销、跨进程令牌写入、动态注册限流和公开路由的安全边界。
- 将遗漏 `finished` 的自动兜底从 45 秒延长为固定十分钟，避免把长任务或静默工作误报为结束。
- 修复 shell 标记跨轮询、进程输出长轮询、参数校验和现代 MCP 请求活动计数。
- 修复多实例端口提示、隧道持有/接管、ngrok/Tailscale 检测与运行中切换提供商。

## [1.0.0-beta.2] — 2026-09-15

### Added

- 完整的设置、会话、工具、健康、服务与安全控制台页面，以及隧道自动检测和诊断能力。
- 公网 MCP 隧道的多实例协调、健康检查、自动重连和运行状态展示。

### Changed

- 工具目录、CLI 与控制台使用同一套运行状态和工具声明；旧工具名继续兼容。
- 文件、进程、服务和网络操作的结果统一提供可供客户端读取的结构化数据。

### Fixed

- 修复认证、令牌轮换、公开访问、工作区路径、资源锁、长驻进程、流式读取和多实例竞态问题。
- 修复控制台的无障碍性、语言一致性、表格布局、轮询竞态和一次性密钥交互。

## [1.0.0-beta.1] — 2026-09-15

### Added

- 增加面向本地工作区的 Web 控制台、实例发现、日志、健康检查和快速接入提示。
- 增加 `run_script`，可在一次调用中组合多个已有 MCP 工具。

### Changed

- 工具面收敛为更少的统一入口，同时保留旧名称与现有工作流的兼容性。
- 生命周期、隧道、会话与路由实现按职责拆分，便于维护。

### Fixed

- 修复文件/补丁安全边界、shell 会话、命令输出、启动/关闭、隧道恢复和构建陈旧提示。

## [1.0.0-alpha.6] — 2026-09-14

### Added

- 增加连接技能发现、工具行为标注、活动追踪和更完整的 API/集成测试覆盖。

### Changed

- 改进公开 URL、对等实例注册、隧道借用与控制台状态信息。

### Fixed

- 修复资源锁、路由轮换、HTTP 超时、运行时记录和跨平台可执行文件定位。

## [1.0.0-alpha.5] — 2026-09-13

### Added

- 增加多页面 Web 控制台、Code Mode、构建陈旧检测和项目级操作入口。

### Changed

- 统一工具目录、运行生命周期和命令行帮助；保留旧 API 名称兼容。

### Fixed

- 修复参数缺失、副作用顺序、目录自毁、Windows 控制台和 `serve --help` 行为。

## [1.0.0-alpha.4] — 2026-09-12

### Added

- 增加工作区 `skills` 发现与 `list_skills` MCP 工具。

## [1.0.0-alpha.3] — 2026-09-12

### Added

- 增加 Windows 双击启动器和 `open-bridge stop` 自停保护。

### Fixed

- 修复关闭终端后残留实例、Windows 信号处理和启动锁清理。

## [1.0.0-alpha.2] — 2026-09-11

### Added

- 增加可选 OAuth 2.1 + PKCE、同一 `/mcp` 端点的双协议支持、日志轮转和会话信息。

### Fixed

- 修复配置持久化、超时参数、并发启动、控制台设置、令牌操作和 CLI 诊断。

## [1.0.0-alpha.1] — 2026-09-11

### Added

- 首次发布：本地工作区 MCP Bridge、CLI、文件/命令/进程工具、配置与密钥存储、ngrok 公网入口和基础 Web 控制台。

### Fixed

- 修复控制台访问、状态/URL 查询、Windows 退出、工具数统计和跨平台 ripgrep 定位。
