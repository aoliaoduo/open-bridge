# Changelog

所有重要变更记录于此。格式遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，版本遵循 [Semantic Versioning](https://semver.org/spec/v2.0.0.html)。

详细的设计过程、根因分析和测试用例请查阅 Git 历史、源码注释与对应文档；本文件只保留面向使用者的发布摘要。

## [Unreleased]

### Changed

- 仓库根目录精简：Windows 启动脚本移到 `scripts/`，架构说明移到 `docs/ARCHITECTURE.md`。`*.cmd` 固定为 CRLF，避免 Linux CI 把启动器误判成 LF。
- TUI 各页标题不再写「Tab 任务 / Tab 变更 / Tab 返回活动」。Tab 仍是切页键，标题只保留当前视图名；滚出顶部时仍显示行数提示。
- TUI 任务面板标题栏增加进度条与完成状态摘要（`▓▓░░░░ 25% · 1/4 完成 · 1 进行中`），在窗口宽度允许时直观展示任务推进比例，翻页滚动时显示完成比与行数定位；进行中的任务行加粗高亮展示。

## [1.0.0] — 2026-09-21

### Added

- TUI Tab 第三页「变更」：列出每个工作区文件的路径与各自 +/-（未跟踪单独标注），侧栏摘要行保留；Tab 在活动 / 任务 / 变更间循环，Esc 仍只回当前页顶部。
- TUI 任务视图：按 Tab 在「活动 / 任务」间切换右侧宽面板。任务视图完整保留任务，长标题按行宽自动折行；↑↓ / PgUp / PgDn / Home / End 滚动当前视图，活动与任务各自保留位置，Tab 仍是唯一切换键（Esc 只回当前视图顶部）。窄窗或矮窗下任务独占内容区，缩放、列表缩短后自动钳制滚动位置；空列表明确显示「暂无任务」。侧栏只保留一行「任务 N（M 进行中）」摘要，不再截断标题。任务与 Web 控制台同源（`set_todos`），重启后从持久化存储恢复。
- TUI 侧栏新增常驻「变更」行：自最近一次 git commit 以来的工作区变更摘要（`+插入 -删除 · N 文件`，新增数绿色、删除数红色，含未跟踪文件按整文件计行），每 5 秒异步刷新、渲染帧只读缓存；干净的工作树显示「干净」而非隐藏（缺失无法区分干净与未监控），非 git 仓库如实显示「非 git」。
- TUI 底栏新增网页控制台入口（`http://127.0.0.1:端口/console/`）：控制台仅应答本机回环请求，入口固定显示本地地址，与 MCP 地址同栏展示。
- 新增文档覆盖回归门禁：docs/tools.md 与 MCP 工具定义强制一一对应——每个已发布的工具必须有文档条目（`name` 或 **name** 两种格式皆可），文档粗体条目也不得承诺未定义的工具。此前这对账只能手工进行，而一次手工 grep 比对就同时漏了 4 个定义、多出 1 个幻影名。
- serve 终端 TUI 工作台布局（阶段三）：宽终端（≥76×22）下左侧固定侧栏（状态/隧道/调用/会话容量/进程/服务概览，每进程输出缓冲水位、服务启停列表）+ 右侧活动面板（↑↓/PgUp/PgDn/Home/End 滚动浏览历史，End/Esc 回到最新并跟随；离开最新时面板标题显示「↑N 行 · End 回底」）。窄窗自动回退堆叠布局。TUI 定位保持只读观看，设置/后台操作仍在 Web 控制台；滚动键经 raw mode 读取，Ctrl+C 在 raw mode 下显式转发真实 SIGINT，优雅关停路径不变，所有终端模式（备用屏/光标/raw stdin）在停止与退出时双重恢复。滚动与几何为纯函数，12 项单测锁定。

### Changed

- 2025 世代 MCP 会话在重启后可恢复：已签发的 `mcp-session-id` 以票根持久化，进程退出或连接断开不会作废；空闲超时与客户端 DELETE 仍使旧 id 失效。从未签发的 id 继续 404。
- 网页 AI 提示词与 MCP 连接说明要求多步工作主动维护 `set_todos`：操作者 TUI 任务面板只显示这份清单；`report_progress` 不能代替它。
- TUI 底栏改为一行一个地址：控制台与 MCP 地址各占整行，小屏幕不再把 MCP 地址截成省略号；同时移除操作提示行（关闭终端窗口即停止，滚动提示由面板标题在滚动时显示）。
- TUI 会话计数不再显示容量上限（如 0/64）：上限是开发者知识，操作者只需知道当前连接数与活跃数。
- TUI 的调用/成功/失败计数改为本次启动以来的计数：持久化的历史统计（跨重启保留、供 Web 控制台诊断）不再进入 TUI，刚启动的实例不再继承旧数字。
- TUI 底栏 MCP 地址完整显示（含路由 token），与启动横幅、`open-bridge url` 输出一致；此前遮蔽为 `<redacted>`，地址无法直接复制使用。
- 移除 TUI 底栏「Ctrl+C 停止」提示：关闭终端窗口即停止服务，提示属于噪音；滚动方式、日志路径与 `--no-tui` 提示保留。

### Fixed

- TUI 活动流改为操作者摘要：隐藏 MCP 传输行和进程生命周期行；参数只抽出 command/query/path，不展示 JSON、timeout 或 `&&` 后续段；`Request received` / `Completed in N ms` 不再占用一行。长消息按空格/路径分隔符折行。
- 修准 TUI 侧栏 Git 变更摘要：未跟踪目录展开为文件计数；空文件与尾换行按 git 行数计（不再被 `split('\n')` 夸大）；含空格等特殊路径不再被 trim/去引号弄丢；二进制未跟踪不计行；git 超时或读取失败显示「读取失败」，不再冒充「非 git」。干净工作树与红绿配色保持不变。
- 修复 TUI 满行写入后再擦除造成的右边界丢字（如「运行中」「Tab 返回活动」尾字消失）：改为逐行绝对定位、先清行后绘制，不依赖换行/自动折返，也不在新内容后执行擦除。
- 修复活动消息、命令摘要等文本携带换行或终端控制序列时串到侧栏、挤掉顶栏的问题。行内文本保留可读内容并净化控制符，任务标题仍按真实换行与列宽折行；颜色由渲染器统一添加。
- 为 `batch` / `run_script` 子调用补齐真实的成功/失败结束记录：已结束的调用不再在 TUI 中永久转圈计时；保留工具返回结构、原始错误及顶层请求的唯一计数口径。
- 修复 TUI 任务摘要只统计前 16 条、后续进行中任务被漏计的问题；任务列表不再受该快照上限截断。任务长标题的图标留白按实际列宽计算，中文终端及多行标题不会挤掉尾字或突破帧高。
- 修复 TUI 活动面板跟随反转：跟随哨兵误钳到数组尾部，而事件数组是最新在前——事件数一旦超过面板行数，跟随会锁死在最旧的事件窗口、新调用不再上屏。现在面板保持最新在顶（向下滚动浏览更旧历史），跟随与 Home/Esc 锁定最新事件，End 跳到最旧历史，离开顶部时面板标题显示「↑N 行 · Home 回顶」；窄窗堆叠布局的活动流同样改为截取最新事件而非最旧。
- TUI 信息去重，一处事实一处：顶栏独占身份/端口/状态胶囊，侧栏独占计数与隧道，底栏独占 MCP 地址与操作提示；移除底栏重复的会话/调用/端口/工作区名与侧栏重复的「状态」字段，无进程时不再渲染空的「进程」段。
- TUI「运行」时长改用本次进程的实际启动时刻：此前误用跨重启持久化的统计窗口起点，Bridge 刚启动也会显示数十小时。
- CJK 终端（zh/ja/ko 区域设置）下模糊宽度字符（·、…、─、●、◆、⚠、✓、✕ 等）按 2 列计宽：盒子边框、分隔列与横向填充线的几何改为按实测字符宽度计算并在截断后补齐，修复行尾字符被终端吃掉（如「● 运行」丢「中」、日志路径截尾缺字符）；非 CJK 终端行为不变。

- 修复 TUI 活动流「永远计时」：invoke 行与结局行的配对此前依赖双方都携带参数摘要，而结局行（由 MCP 端点记录）从不携带——配对永不成立，每个已完成的工具调用都显示为永久转圈的进行中，计时虚涨；`process · Started` 生命周期行则根本没有对应的结局行。现在结局行按工具名 FIFO 关闭最老的未闭合 invoke 并显示真实耗时；`process` 行改从命令表读取真实状态——存活进程照常计时，已结束进程显示真实存活时长（endedAt−startedAt），已清理的记录降级为中性标记、不虚构时间。仅影响显示，审计日志不变。
- serve 终端新增 ainovel-cli 风格的 TUI 仪表盘（阶段一）：启动横幅之后进入备用屏幕，500ms 就地重绘——顶栏（版本 · 端口 · 隧道 · 工作区名 · 状态胶囊，繁忙时胶囊图标切换为实时 spinner）、概览卡（运行时长 / 调用 / 会话容量 / 进程 / 服务）、进程卡（运行中命令 + 输出缓冲水位条）、活动流（时间戳 + ✓/✕/spinner 三态图标；invoke 行在其结局落地时合并为携带真实耗时的一行，未观测到配对则不虚构耗时）、底部用量状态栏。健康度渐变沿用 ainovel-cli 的阈值（<70% 绿 / 70–84% 黄 / ≥85% 红），应用于会话容量与每命令输出缓冲占用。Ctrl+C 语义不变（无 raw mode），停止时恢复原始终端；无真实控制台（服务 / CI / 重定向）或 `--no-tui` 时保持原输出路径。渲染层为纯函数（CJK 感知列宽、精确几何、窄窗降级阶梯），由单测锁定。

## [1.0.0-rc.2] — 2026-09-20

### Changed

- `list_directory` / `search_files` 的已公布 inputSchema 与运行时对齐：`depth` 移除 1–3 枚举（小于 1 钳制为 1、更大的值按请求递归），`context` 移除 0–20 上下限（超界钳制）——运行时接受的合法调用不再违反自己公布的契约。
- 控制台统计页措辞诚实化：调用计数按工作区持久化、跨 Bridge 重启保留，「本次运行」改为「自统计开始」、「运行时长」改为「统计时长」。
- docs/tools.md 补齐：`open_shell` 新开/复用两种结果形状、`send_to_shell` 结果 `cwd` 的含义（启动目录，不跟踪 shell 内 `cd`）、`write_file` 响应 `mode` 的取值、`get_usage_stats` 的统计窗口语义、`run_script` 预算钳制区间。

### Fixed

- `find_files` 的 `**/` 现在只按**完整路径段**匹配（或匹配零段）：`**/host.ts` 不再误中 `node-host.ts`；裸 `**` 照旧可跨分隔符。
- 创建/删除文件的结果 diff（`apply_patch`、`edit_block` 等）不再产生幻影空行与多计一次增删：空的一侧按 0 行处理，`\ No newline at end of file` 只在真实缺少尾换行时出现，与 git 一致。
- `file_op{op:"create_directory"}` 的 `created` 如实反映**本次调用**是否建了目录：目录已存在时返回 `created: false`（此前恒为 `true`）。
- `write_file` 的 `mode` 与 `read_files` 的 `encoding` 收到非法值时报 `Invalid` 并列出合法值，不再静默回退（此前 `mode:"Append"` 会按覆盖写、拼错的 `encoding` 会按 utf8 读二进制）。
- `activity_log{action:"recent"}` 的 `at` 与 `search`、审计日志、`/api/activity` 一致，为 ISO-8601 UTC（此前是服务器本地时间串）；控制台活动视图仍显示本地时间（改在渲染层格式化）。
- 文档与实现对齐：`read_files` 的 `sha256` 尾部预算行为、`edit_block` 的 `replace_all` 参数、`process_control{action:"restart"}` 返回新 `command_id` 均已写明。
- 修复 Windows + Git Bash 下停止/重启/删除被监控命令时误报「无法终止」的问题：MSYS 的 fork/exec 模拟会切断 Windows 父子链接，后台子进程（如 `while` 循环里的 `sleep 30 &`）成为 `taskkill /T` 与进程树枚举都看不见的孤儿，占住 stdio 管道导致 close 永不触发、5 秒预算耗尽。现在对 bash 类 shell 先枚举 Windows 可见成员，再经 `ps -W` 的 PGID 按 MSYS 进程组整组 `kill -9`（严格跳过组 0，防止波及无关系统进程），最后 taskkill 收尾；原生 shell（PowerShell/cmd）改为单次原子 `taskkill /T /F`。附 win32 门控的进程树集成测试。
- 修复 `close_shell` 泄漏后台任务：Windows + Git Bash 下关闭持久 shell 返回 `closed:true`，但会话里的后台任务（`npm run dev &`、`sleep 300 &` 等）继续存活——bash 优雅退出不带走后台子进程，令整个 kill 块被跳过；即使执行到，`taskkill /T` 也看不见 MSYS 孤儿。现在 win32 的 bash 类会话直接进入与被监控命令终止共享的家族击杀路径（`killWindowsProcessFamily`：枚举 → MSYS 进程组组杀 → taskkill），两处不再各自漂移。附 win32 门控集成测试（改名副本 ob-sleep 防并行文件干扰）。
- 修复 `open-bridge stop` 兜底路径泄漏后台任务：目标实例无法响应 HTTP 优雅停止时，CLI 直接`taskkill /T /F` 实例 pid——只能杀到 Windows 可见进程树，实例 bash 会话/受管命令的 MSYS 孤儿（`npm run dev &`、watcher）继续存活为无主游魂（与 terminateProcess/close_shell 同根因，第三处同型点）。家族击杀逻辑抽出为无 host 依赖的 `src/process/win-family-kill.ts`，terminateProcess、closeShell 与 CLI 兜底三处共用同一实现；CLI 侧以 autoShell()（与未配置时桥自身解析的首选 shell 一致）执行组清扫，并在事后核验实例确实退出而非信任被吞掉的逐成员错误。附 win32 门控集成测试（runtime 记录指向死端口模拟挂死 + 改名副本 cli-orphan 防并行文件干扰）。

## [1.0.0-rc.1] — 2026-09-20

### Added

- 新增文档回归检查，识别失效的源码指针和示例中的真实 MCP 连接凭据；现行文档变更进入 CI 验证。
- 新增 `npm run package:check` 与 `npm run release:check`：发布前检查 npm 实际打包清单，确保 CLI、编译产物和用户文档齐全，且不误发源码、测试和工作流文件；CI 同步执行该检查。
- 为长命令的后台监管结果新增 `ready_checked`，明确区分“已观察到就绪”与“未请求就绪检查”；工具契约和文档同步说明基于 `command_id` 续读，避免传输超时后重发有副作用的命令。
- 补全 `get_config` 的 MCP 输出 schema：工具档、认证、OAuth、并发、通知、声音和日志等实际返回字段现均有机器可读类型声明。
- 新增可选的 **Tailscale Funnel** 公网隧道提供商；保留既有 ngrok 与 `--no-tunnel` 路径。
- 新增 `start-open-bridge-project.cmd`：双击后固定以本项目为工作区、使用端口 **8123**，构建后在终端显示控制台地址但不自动打开浏览器。

### Changed

- 精简 Agent/贡献/PR 约定，区分缺陷回归与新增覆盖，并明确说明注入、任务授权及重启边界；问题报告模板提醒先脱敏。
- 移除无生产调用的旧通知、控件元数据、单注册表查找和数组搜索上下文实现；测试改为验证真实 schema、设置入口和搜索结果，测试夹具不再随运行代码发布。
- 清理已过时的迁移/对照待办，更新架构、接入、路由令牌与日志存储说明；协作指南改为不含实例凭据和个人机器路径的可复用流程。
- 移除无调用者的内部 CLI 语言设置入口，保留环境变量/系统 locale 的语言选择，以及现有协议与工具别名兼容。
- 手机通知收敛为两个明确事件：`waiting`（真正等待回答/选择）和 `finished`（本轮结束）。每个工作 episode 最多一次 Bark 请求，固定使用持续、time-sensitive 的投递形态。
- 对会暂停回合的提问/选择，连接指令要求在展示 UI 前或同一回合发送 `waiting`。
- 控制台隧道设置统一为“提供商、状态与操作、高级设置”三段；自动配置只填空值，公网可达性可在控制台验证。
- 连接时会说明实际用于执行命令的 shell 与方言，避免模型按错误语法运行命令。

### Removed

- 移除 Electron 桌面客户端及其构建链；项目保持 CLI + MCP 服务 + Web 控制台。
- 移除 MCP 工具结果中的“行为教练”提示层。工具、todo 看板和批处理能力不受影响，但模型不再被连续调用提示和过时的 todo 通知说法打扰。
- 移除旧的通知路由、重复投递、进度/注意力事件和 todo 自动推送配置。

### Fixed

- 修复控制台 ngrok 手动域名无法保存、已保存的自定义域名刷新后隐藏，以及无法显式清除域名的问题；明确空值在下次启动时仅本机可用，移除不受支持的随机地址承诺，保留严格主机名校验。
- 修复较早的保存回包覆盖新输入草稿；手动域名保存失败或保存期间继续输入时保留当前草稿。
- 公网可达测试必须有成功的公网探测才显示可达；未发布、未探测与探测失败分开显示，保留暴露风险提示。
- 会话页补充现代 MCP 无状态活动汇总与进行中请求数，区分首次观察和握手时间；不虚构会话 ID、调用/待办总数或断开操作，状态页明确仅统计有状态会话。
- 修复会话断开或回收后任务看板仍显示实时的问题，保留历史计划与进展并标注离线。
- 服务操作按服务分别防重入，允许不同服务并行操作，完成或失败不会提前解锁其他仍在处理的服务。
- 控制台写操作遇到无效成功响应时明确提示结果未知并保留原页面状态，不自动重发可能已经执行的请求。


- 修复通知设置页：折叠与展开状态都显示「手机/本机声音」状态徽章；Bark 设备密钥在锁定时新增只读视觉反馈；按钮 disabled 同时降饱和度便于辨识；「什么时候提醒你」从可折叠改为静态描述卡（无可揭示内容）；Bark 密钥掩码格式改为 `前缀4 … 8 个点 … 后缀2`，避免前后省略号与圆点合并。新增对应 UI 测试。

- 审计依赖、打包清单与发布资产：`@modelcontextprotocol/sdk`、`@modelcontextprotocol/server`、`@modelcontextprotocol/node` 各自是必要的 v1/v2 协议直接依赖；其余 devDep 全部由 vitest/vite/jsdom 与 typescript-eslint 实际使用，无可删除的冗余。`scripts/`、`vendor/`、`package.json` `files` 与 package-check 三方一致；当前没有发现需要删除或更新的过期资产。
- 补全行为准则遗留的举报联系方式占位符，使用项目已公开的维护者联系渠道，并添加回归检查。
- 加固文件复制、补丁、路径、自毁保护和换行处理，避免静默覆盖或错误 diff。
- 修复 OAuth 令牌撤销、跨进程令牌写入、动态注册限流和公开路由的安全边界。
- 将遗漏 `finished` 的自动兜底从 45 秒延长为固定十分钟，避免把长任务或静默工作误报为结束。
- 修复 shell 标记跨轮询、进程输出长轮询、参数校验和现代 MCP 请求活动计数。
- 修复多实例端口提示、隧道持有/接管、ngrok/Tailscale 检测与运行中切换提供商。

## [1.0.0-beta.2] — 2026-09-15

### Added

- 完整的设置、会话、工具、健康、服务与安全控制台页面，以及隧道自动检测和诊断能力。
- 公网 MCP 隧道的多实例协调、健康检查、自动重连和运行状态展示。

### Changed

- 工具目录、CLI 与控制台使用同一套运行状态和工具声明；旧工具名继续兼容。
- 文件、进程、服务和网络操作的结果统一提供可供客户端读取的结构化数据。

### Fixed

- 修复认证、令牌轮换、公开访问、工作区路径、资源锁、长驻进程、流式读取和多实例竞态问题。
- 修复控制台的无障碍性、语言一致性、表格布局、轮询竞态和一次性密钥交互。

## [1.0.0-beta.1] — 2026-09-15

### Added

- 增加面向本地工作区的 Web 控制台、实例发现、日志、健康检查和快速接入提示。
- 增加 `run_script`，可在一次调用中组合多个已有 MCP 工具。

### Changed

- 工具面收敛为更少的统一入口，同时保留旧名称与现有工作流的兼容性。
- 生命周期、隧道、会话与路由实现按职责拆分，便于维护。

### Fixed

- 修复文件/补丁安全边界、shell 会话、命令输出、启动/关闭、隧道恢复和构建陈旧提示。

## [1.0.0-alpha.6] — 2026-09-14

### Added

- 增加连接技能发现、工具行为标注、活动追踪和更完整的 API/集成测试覆盖。

### Changed

- 改进公开 URL、对等实例注册、隧道借用与控制台状态信息。

### Fixed

- 修复资源锁、路由轮换、HTTP 超时、运行时记录和跨平台可执行文件定位。

## [1.0.0-alpha.5] — 2026-09-13

### Added

- 增加多页面 Web 控制台、Code Mode、构建陈旧检测和项目级操作入口。

### Changed

- 统一工具目录、运行生命周期和命令行帮助；保留旧 API 名称兼容。

### Fixed

- 修复参数缺失、副作用顺序、目录自毁、Windows 控制台和 `serve --help` 行为。

## [1.0.0-alpha.4] — 2026-09-12

### Added

- 增加工作区 `skills` 发现与 `list_skills` MCP 工具。

## [1.0.0-alpha.3] — 2026-09-12

### Added

- 增加 Windows 双击启动器和 `open-bridge stop` 自停保护。

### Fixed

- 修复关闭终端后残留实例、Windows 信号处理和启动锁清理。

## [1.0.0-alpha.2] — 2026-09-11

### Added

- 增加可选 OAuth 2.1 + PKCE、同一 `/mcp` 端点的双协议支持、日志轮转和会话信息。

### Fixed

- 修复配置持久化、超时参数、并发启动、控制台设置、令牌操作和 CLI 诊断。

## [1.0.0-alpha.1] — 2026-09-11

### Added

- 首次发布：本地工作区 MCP Bridge、CLI、文件/命令/进程工具、配置与密钥存储、ngrok 公网入口和基础 Web 控制台。

### Fixed

- 修复控制台访问、状态/URL 查询、Windows 退出、工具数统计和跨平台 ripgrep 定位。
